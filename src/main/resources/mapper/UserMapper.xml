<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.music.dao.UserMapper" >
    <resultMap id="BaseResultMap" type="com.zh.music.bean.User" >
        <result column="id" property="id" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="TINYINT" />
        <result column="phone_num" property="phoneNum" jdbcType="CHAR" />
        <result column="email" property="email" jdbcType="CHAR" />
        <result column="birth" property="birth" jdbcType="TIMESTAMP" />
        <result column="introduction" property="introduction" jdbcType="VARCHAR" />
        <result column="location" property="location" jdbcType="VARCHAR" />
        <result column="avatar" property="avatar" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="forbidden" property="forbidden" jdbcType="TINYINT" />
    </resultMap>
    <sql id="Base_Column_List" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, username, password, sex, phone_num, email, birth, introduction, location, avator, create_time, update_time,forbidden
    </sql>
    <select id="userOfId" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
        select
        <include refid="Base_Column_List" />
        from consumer
        where id = #{id}
    </select>
    <select id="verifyPassword" resultType="java.lang.Integer">
        SELECT count(*) FROM consumer where username = #{0} and password = #{1} ;
    </select>
    <select id="existUsername" resultType="java.lang.Integer" parameterType="java.lang.String">
        select count(*)
        from consumer
        where username = #{username}
    </select>
    <select id="loginStatus" resultMap="BaseResultMap">
        select *
        from consumer
        where username = #{username}
    </select>
    <select id="allUser" resultMap="BaseResultMap">
        select *
        from consumer
    </select>

    <update id="forbidByPrimaryKey" parameterType="java.lang.Integer">
        update consumer
        set forbidden =#{forbidden}
        where id=#{id}
    </update>
</mapper>